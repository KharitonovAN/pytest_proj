[tool.poetry]
name = "зpytest-proj"
version = "0.1.0"
description = "solution through зpytest"
authors = ["HAN <han82@mail.ru>"]
license = "-"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
